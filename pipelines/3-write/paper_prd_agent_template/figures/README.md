# Figures

Place repository-generated figures in this directory.

Default inclusion rule:

```latex
\paperfig{figure_file.pdf}{Caption text.}{fig:label}
```

The `\paperfig` macro uses `0.8\columnwidth` by default. Use another width only when necessary for readability, and explain the exception in a LaTeX comment.
