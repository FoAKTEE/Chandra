{
  "name": "chandra-orchestrator",
  "version": "0.1.0",
  "description": "v2 runtime: topological scheduler over the mission DAG, parallel SDK workers, isolated validators, observer memory agent, 5-window human digest cadence",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "tsc -p .",
    "test": "npm run build --silent && node --test \"dist/test/*.test.js\"",
    "plan": "node dist/src/main.js plan",
    "run-mission": "node dist/src/main.js run"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.201"
  },
  "devDependencies": {
    "@types/node": "^18.19.130",
    "typescript": "^5.9.3"
  }
}
